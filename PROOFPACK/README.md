# Proofpack (Template)

This folder is the tamper-evident artifact layer.

- `MANIFEST.json` lists sha256 hashes for tracked files
- `RESULTS/` holds reproducible outputs generated by `CODE/`
- CI verifies the manifest matches the repo contents

Rule: if it won’t verify, it doesn’t count.
