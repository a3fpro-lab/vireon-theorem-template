# Algorithms (Template)

Place algorithms, update rules, pseudocode, and complexity notes here.

If an algorithm is asserted correct:
- link the correctness claim to a theorem/lemma in `THEOREM.md` or `PROOF.md`
- provide an implementation under `CODE/`
- store outputs under `PROOFPACK/RESULTS/` and keep them hashed in the manifest
